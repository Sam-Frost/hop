Simple TUI applicantion to store all the servers ips & pems file allowing easy ssh access

Future Features : 
- Add Workspaces with option of default workspace
- Add parameters in commandline to add default pem path
- Don't show pem path in the servers ui
- Don't show default pem file
- Clear the ui when starting something new
